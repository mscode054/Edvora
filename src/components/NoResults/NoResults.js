export const NoResults = () => {
    const noResultsCss = {
        textAlign: "center",
        color: "white",
        padding: "5rem 0",
    };
    return (
        <div style={noResultsCss} >No Result</div>
    );
};